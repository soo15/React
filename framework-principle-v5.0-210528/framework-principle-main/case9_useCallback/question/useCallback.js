// (option) memoryCells & cursor
// const memoryCells = {}
// let cursor = 0

// (option) mocking react event system
document.addEventListener('initializeCallback', () => {

})

function useCallback(targetCallback, dependencyArray) {
  // 여기에 useCallback 함수의 로직을 작성해보세요.
}

export default useCallback

// debugging 용
window.memoryCells = memoryCells
