// (option) memoryCells & cursor
const memoryCells = {}
let cursor = 0

// (option) mocking react event system
document.addEventListener('initialize', () => {

})

const useRef = (initialValue) => {
  // 여기에 useRef 함수의 로직을 작성해보세요.
}

export default useRef

// debugging 용
window.memoryCells = memoryCells
